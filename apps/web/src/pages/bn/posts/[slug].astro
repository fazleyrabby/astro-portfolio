---
import Page from '../../posts/[slug].astro';
import { getPostSlugs } from '../../../lib/supabase-cache';

export async function getStaticPaths() {
  const posts = await getPostSlugs();
  
  return posts.map((post) => ({
    params: { slug: post.slug },
  }));
}
---
<Page {...Astro.props} />
