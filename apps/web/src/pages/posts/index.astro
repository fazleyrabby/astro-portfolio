---
import MainLayout from "@layouts/MainLayout.astro";
import { getAllPosts } from "@lib/supabase-cache";
import {
  getLangFromUrl,
  useTranslations,
  useTranslatedPath,
} from "../../i18n/utils";

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
const translatePath = useTranslatedPath(lang);

const allPosts = await getAllPosts(lang);

let publishedPosts: any[] = [];
if (allPosts) {
  if (lang === "bn") {
    const bnPosts = allPosts.filter((p) => p.lang === "bn");
    const translatedEnIds = new Set(
      bnPosts.map((p) => p.translation_of).filter(Boolean),
    );
    publishedPosts = allPosts.filter((p) => {
      if (p.lang === "bn") return true;
      if (p.lang === "en" && !translatedEnIds.has(p.id)) return true;
      return false;
    });
  } else {
    publishedPosts = allPosts.filter((p) => p.lang === "en");
  }
}

const featuredPost = publishedPosts[0];
const archivePosts = publishedPosts.slice(1);
---

<MainLayout title="Engineering Journal & Notes — Fazley Rabbi">
  <main class="posts-page">
    <div class="posts-container">

      <!-- Header -->
      <header class="posts-header reveal">
        <span class="page-kicker">Writing</span>
        <h1 class="page-title">Engineering Journal</h1>
        <p class="page-sub">Deep dives into production Laravel patterns, system architecture, and technical challenges solved in the real world.</p>
      </header>

      {publishedPosts.length === 0 ? (
        <div class="posts-empty">{t("posts.emptyMsg") || "Synchronizing brain dump... no articles found yet."}</div>
      ) : (
        <div class="posts-layout">

          <!-- Featured post -->
          {featuredPost && (
            <article class="post-featured reveal">
              <div class="post-featured-body">
                <div class="post-meta">
                  <span class="post-badge">Featured Article</span>
                  <span class="post-date">
                    {featuredPost.published_at
                      ? new Date(featuredPost.published_at).toLocaleDateString(lang === "bn" ? "bn-BD" : "en-US", {
                          month: "long",
                          day: "numeric",
                          year: "numeric"
                        })
                      : "Draft"}
                  </span>
                </div>

                <h2 class="post-featured-title">
                  <a href={translatePath(`/posts/${featuredPost.slug}`)}>{featuredPost.title}</a>
                </h2>

                <p class="post-featured-desc">{featuredPost.description}</p>

                {featuredPost.tags && featuredPost.tags.length > 0 && (
                  <div class="post-tags">
                    {featuredPost.tags.map((tag: string) => (
                      <span class="tag">{tag}</span>
                    ))}
                  </div>
                )}

                <a href={translatePath(`/posts/${featuredPost.slug}`)} class="post-read-link">
                  Read Article
                  <svg width="14" height="14" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M13.5 4.5L21 12m0 0l-7.5 7.5M21 12H3"/></svg>
                </a>
              </div>
            </article>
          )}

          <!-- Archive list -->
          {archivePosts.length > 0 && (
            <div class="posts-archive reveal">
              <h2 class="archive-heading">All Articles</h2>
              <div class="archive-list">
                {archivePosts.map((post: any) => (
                  <article class="archive-item">
                    <div class="archive-main">
                      <h3 class="archive-title">
                        <a href={translatePath(`/posts/${post.slug}`)}>{post.title}</a>
                      </h3>
                      <p class="archive-desc">{post.description}</p>
                    </div>

                    <div class="archive-meta">
                      <span class="archive-date">
                        {post.published_at
                          ? new Date(post.published_at).toLocaleDateString(lang === "bn" ? "bn-BD" : "en-US", {
                              month: "short",
                              year: "numeric"
                            })
                          : "Draft"}
                      </span>
                    </div>
                  </article>
                ))}
              </div>
            </div>
          )}

        </div>
      )}

    </div>
  </main>
</MainLayout>

<style>
  .posts-page {
    padding: clamp(2.5rem, 6vw, 5rem) 0;
  }
  .posts-container {
    max-width: 960px;
    margin: 0 auto;
    padding: 0 clamp(1.25rem, 4vw, 2rem);
  }

  .posts-header {
    margin-bottom: 3rem;
  }

  .posts-empty {
    text-align: center;
    padding: 4rem 0;
    font-family: var(--font-sans);
    color: var(--text-muted);
  }

  .posts-layout {
    display: flex;
    flex-direction: column;
    gap: 3.5rem;
  }

  /* Featured Article */
  .post-featured {
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
    background: var(--bg-card);
    padding: clamp(1.5rem, 4vw, 2.5rem);
    transition: border-color 0.3s ease;
  }
  .post-featured:hover {
    border-color: var(--border-hover);
  }

  .post-meta {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    margin-bottom: 1rem;
  }
  .post-badge {
    font-family: var(--font-mono);
    font-size: 0.6875rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--accent);
    background: var(--accent-dim);
    padding: 0.2rem 0.6rem;
    border-radius: 4px;
  }
  .post-date {
    font-family: var(--font-mono);
    font-size: 0.75rem;
    color: var(--text-muted);
  }

  .post-featured-title {
    font-family: var(--font-display);
    font-weight: 500;
    font-size: clamp(1.375rem, 2.5vw, 1.875rem);
    color: var(--text-primary);
    line-height: 1.2;
    margin: 0 0 0.875rem;
    letter-spacing: -0.02em;
  }
  .post-featured-title a {
    color: inherit;
    text-decoration: none;
    transition: color 0.2s ease;
  }
  .post-featured-title a:hover {
    color: var(--accent);
  }

  .post-featured-desc {
    font-family: var(--font-sans);
    font-size: 1rem;
    color: var(--text-secondary);
    line-height: 1.65;
    margin: 0 0 1.25rem;
  }

  .post-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.375rem;
    margin-bottom: 1.5rem;
  }

  .post-read-link {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    font-family: var(--font-sans);
    font-size: 0.875rem;
    font-weight: 500;
    color: var(--accent);
    text-decoration: none;
    transition: opacity 0.2s ease;
  }
  .post-read-link:hover {
    opacity: 0.8;
  }

  /* Archive */
  .archive-heading {
    font-family: var(--font-display);
    font-weight: 500;
    font-size: 1.5rem;
    color: var(--text-primary);
    margin: 0 0 1.5rem;
    padding-bottom: 0.5rem;
    border-bottom: 1px solid var(--border);
    letter-spacing: -0.01em;
  }

  .archive-list {
    display: flex;
    flex-direction: column;
  }

  .archive-item {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    gap: 2rem;
    padding: 1.25rem 0;
    border-bottom: 1px solid var(--border);
  }
  @media (max-width: 600px) {
    .archive-item {
      flex-direction: column;
      gap: 0.5rem;
    }
  }

  .archive-main {
    flex: 1;
  }
  .archive-title {
    font-family: var(--font-display);
    font-weight: 500;
    font-size: 1.25rem;
    color: var(--text-primary);
    margin: 0 0 0.375rem;
    line-height: 1.25;
  }
  .archive-title a {
    color: inherit;
    text-decoration: none;
    transition: color 0.2s ease;
  }
  .archive-title a:hover {
    color: var(--accent);
  }

  .archive-desc {
    font-family: var(--font-sans);
    font-size: 0.875rem;
    color: var(--text-secondary);
    line-height: 1.55;
    margin: 0;
  }

  .archive-meta {
    white-space: nowrap;
    padding-top: 0.25rem;
  }
  .archive-date {
    font-family: var(--font-mono);
    font-size: 0.75rem;
    color: var(--text-muted);
  }
</style>
